<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adtip.app.adtip_app"> <!-- IMPORTANT: REPLACE WITH YOUR ACTUAL PACKAGE NAME (e.g., com.myawesomeapp) -->

    <!-- BASIC NETWORK PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- AGORA VOICE/VIDEO CALL PERMISSIONS -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- For Android 14 (API 34) and above, if using microphone/camera in a foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" tools:targetApi="upsideDownCake"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" tools:targetApi="upsideDownCake"/>
    <uses-permission android:name="android.permission.BROADCAST_STICKY" /> <!-- Useful for sticky broadcasts related to services -->

    <!-- BLUETOOTH PERMISSIONS (For routing audio to Bluetooth devices) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- Required for Android 12 (API level 31) and higher for Bluetooth scanning/connecting -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
                     android:usesPermissionFlags="neverForLocation"
                     tools:targetApi="s" />

    <!-- LOCATION PERMISSIONS (Already present, good to keep if your app needs them) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- PHONE STATE & CALLS (Already present, essential for call management features) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <!-- If you need to make outbound calls directly from the app (e.g., using dialer) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- STORAGE PERMISSIONS -->
    <!-- For older Android versions (API 28 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission 
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" 
        tools:replace="android:maxSdkVersion" />
    <!-- For Android 10 (API 29) and higher, use scoped storage or these granular permissions for media -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:targetApi="tiramisu" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:targetApi="tiramisu" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:targetApi="tiramisu" />

    <!-- NOTIFICATIONS (Already present, crucial for push notifications and call notifications) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="tiramisu" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" /> <!-- For high-priority incoming call notifications -->

    <!-- WAKE LOCK (Already present, important for keeping CPU awake during calls) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- AD TRACKING (Already present) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- SMS (for OTP verification - Already present) -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:name=".MainApplication" 
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"> <!-- IMPORTANT: Allows HTTP traffic for development/local servers -->

        <!-- Enable Scoped Storage for Android 10+ (Keep this for compatibility) -->
        <meta-data
            android:name="android:requestLegacyExternalStorage"
            android:value="true" />

        <!-- AdMob App ID (replace with your actual AdMob App ID) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1099807384885107~8206076548"
            tools:replace="android:value" />

        <activity
            android:name=".MainActivity" 
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"> <!-- Essential for Android 12+ -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Linking (example for react-native-inappbrowser or react-native-webview) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="adtip" android:host="app" />
            </intent-filter>
        </activity>

        <!-- Firebase Messaging Service (Already present) -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- For handling incoming call notifications when app is in background -->
        <service
            android:name=".CallNotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- This ensures the app can be launched from notifications when closed -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="call_channel" />

        <!-- Add any other services, receivers, or providers here if your app uses them -->

    </application>

</manifest>